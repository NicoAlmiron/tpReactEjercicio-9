import React from "react";

const AvatarMascota = ({ listaPacientes }) => {
  return (
    <div
      style={{
        width: "40px",
        height: "40px",
        background: "black",
        borderRadius: "3rem",
      }}
      className="me-2 mt-2"
    ></div>
  );
};

export default AvatarMascota;
